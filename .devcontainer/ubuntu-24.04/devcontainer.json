{
    "name": "Ubuntu 24.04",
    "image": "mcr.microsoft.com/vscode/devcontainers/base:0-lunar",
    "runArgs": ["--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined"],
    "settings": {},
    "extensions": [],
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y build-essential cmake git curl wget vim nano python3 python3-pip gdb lldb clang-format && echo 'Setup for Ubuntu 24.04 complete'"
}
